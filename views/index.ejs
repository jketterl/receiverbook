<h1>Receiver list</h1>
<table class="table mt-5">
    <% receivers.forEach((receiver) => { %>
        <tr>
            <td>
                <h5><%= receiver.label %></h5>
                <div><a href="<%= receiver.url %>" target="_blank"><%= receiver.url %></a></div>
                <div><%= receiver.getPresentationType() %> <%= receiver.version %></div>
            </td>
        </tr>
    <% }); %>
</table>